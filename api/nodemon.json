{
  "watch": ["src"],
  "ignore": ["src/**/*.test.ts"],
  "ext": "ts,js,json",
  "exec": "esbuild `find src \\( -name '*.ts' \\)` --platform=node --outdir=dist --format=cjs && node dist",
  "legacyWatch": true
}